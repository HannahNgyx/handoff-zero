# Medplum (public — used by browser client for FHIR metadata + later auth)
NEXT_PUBLIC_MEDPLUM_BASE_URL=https://api.medplum.com/
NEXT_PUBLIC_MEDPLUM_CLIENT_ID=

# Server Client Application (Phase 2 FHIR writes). Without these, Transmit uses in-memory store.
MEDPLUM_CLIENT_ID=
MEDPLUM_CLIENT_SECRET=

# DEEPGRAM_API_KEY=
